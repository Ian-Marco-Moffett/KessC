{
  u8 i = 0;           // Variable 'i' starts at 0.
  while (i <= 10) {   // Loop while 'i' <= 10.
    if (i > 4) {      // Only print out 'i' when it is > 4.
      out(i);
    }

    i = i + 1;        // Increment 'i', don't wanna be in a infinite loop!
  }
}
